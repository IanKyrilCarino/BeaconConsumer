networks:
  beacon-dev:
    external: true

services:
  ian-consumer:
    container_name: ian-consumer
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ..:/workspace:cached      
    command: sleep infinity
    networks:
      - beacon-dev

volumes:
  vscode_extensions:
    external: true
  vscode_data:
    external: true
