networks:
  beacon-dev:
    external: true

services:
  ian_consumer:
    container_name: ian_consumer
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ..:/app:cached      
    command: sleep infinity
    networks:
      - beacon-dev

volumes:
  vscode_extensions:
    external: true
  vscode_data:
    external: true
